class Slingshot{
    constructor(bodyA,bodyB){
        var options={
          isStatic=1.5
        }

    }
    this.sling = Constraint.create(options);
        this.pointB=pointB;
        World.add(world, this.sling);
}
